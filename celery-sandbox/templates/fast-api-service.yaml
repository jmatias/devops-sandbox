apiVersion: v1
kind: Service
metadata:
  name: fast-api
  labels:
    {{- include "celery-sandbox.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - protocol: TCP
      port: 5000
      targetPort: 5000
  selector:
    app.kubernetes.io/name: fast-api
