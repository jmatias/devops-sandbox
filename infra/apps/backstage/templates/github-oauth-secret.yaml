apiVersion: external-secrets.io/v1beta1
kind: ExternalSecret
metadata:
  name: github-oauth
spec:
  refreshInterval: 24h
  secretStoreRef:
    name: cluster-secretstore
    kind: ClusterSecretStore
  target:
    name: github-oauth
    creationPolicy: Owner
  data:
    - secretKey: AUTH_GITHUB_CLIENT_ID
      remoteRef:
        key: javier/backstage/github-oauth
        property: AUTH_GITHUB_CLIENT_ID

    - secretKey: AUTH_GITHUB_CLIENT_SECRET
      remoteRef:
        key: javier/backstage/github-oauth
        property: AUTH_GITHUB_CLIENT_SECRET

    - secretKey: AUTH_GITHUB_TEST_CLIENT_ID
      remoteRef:
        key: javier/backstage/github-oauth
        property: AUTH_GITHUB_CLIENT_ID

    - secretKey: AUTH_GITHUB_TEST_CLIENT_SECRET
      remoteRef:
        key: javier/backstage/github-oauth
        property: AUTH_GITHUB_CLIENT_SECRET
