apiVersion: v1
kind: Service
metadata:
  name: fast-api
  annotations:
    external-dns.alpha.kubernetes.io/hostname: api.tw-javier.work
spec:
  ports:
    - protocol: TCP
      port: 5000
      targetPort: 5000
  selector:
    app.kubernetes.io/name: fast-api
