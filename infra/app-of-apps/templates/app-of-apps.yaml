apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: app-of-apps
  namespace: argocd
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  destination:
    namespace: argocd
    server: https://kubernetes.default.svc
  project: default
  source:
    path: infra/app-of-apps
    repoURL: {{ .Values.spec.source.repoURL }}
    targetRevision: {{ .Values.spec.source.targetRevision }}
    helm:
      valueFiles:
        - values.yaml
  syncPolicy:
    syncOptions:
      - CreateNamespace=true
      - ServerSideApply=true

    automated:
      enabled: {{ .Values.appOfApps.autoSync }}
      prune: true
      selfHeal: {{ .Values.appOfApps.autoSync }}
      allowEmpty: false

    retry:
      backoff:
        duration: 5s
        factor: 2
        maxDuration: 5m0s
      limit: 15
