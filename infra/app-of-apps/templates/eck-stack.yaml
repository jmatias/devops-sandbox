# infra/app-of-apps/templates/eck-stack.yaml
{{ if .Values.eckOperator.enabled }}

apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: eck-stack
  namespace: argocd
  annotations:
    argocd.argoproj.io/compare-options: IgnoreExtraneous
    argocd-image-updater.argoproj.io/write-back-method: git
    argocd-image-updater.argoproj.io/image-list: docker.elastic.co/beats/filebeat, docker.elastic.co/beats/metricbeat, docker.elastic.co/kibana/kibana
    argocd-image-updater.argoproj.io/update-strategy: semver
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  destination:
    namespace: elastic-stack
    server: {{ .Values.spec.destination.server }}
  project: default
  source:
    path: infra/apps/eck-stack
    repoURL: {{ .Values.spec.source.repoURL }}
    targetRevision: main
    kustomize:
      namespace: elastic-stack
  syncPolicy:
    syncOptions:
      - CreateNamespace=true
      - Prune=true
      - ServerSideApply=true
    {{- if .Values.eckOperator.autoSync }}
    automated:
      prune: true
      selfHeal: true
    {{- end }}
    retry:
      backoff:
        duration: 5s
        factor: 2
        maxDuration: 5m
      limit: 15

{{- end }}
